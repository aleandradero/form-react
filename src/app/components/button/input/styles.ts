import { StyleSheet } from "react-native";
import Input from ".";

const styles = StyleSheet.create({
    Input:{
    width: "100%",
    height: 52,
    borderRadius: 10,
    borderWidth: 1,
    borderColor:"#0000CD",
    color: "#0000CD",
    fontSize: 16,
    padding: 12,
},
label: {
    fontSize: 14,
    color: "#0000CD",
},
});

export default styles;